[global]
type=global
user_agent=Home Assistant Asterisk 22
endpoint_identifier_order=ip,username,anonymous

[transport-udp]
type=transport
protocol=udp
bind=0.0.0.0:__SIP_PORT__
allow_reload=yes

#include pjsip_gui.conf
